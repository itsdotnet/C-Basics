
int main()
{
    /*
    FILE *lol = fopen("txtxtxt.txt", "r+");
    FILE *bin = fopen("toBin.txt", "w");
    char h;
    char hehe[50] = {};
    int binne;
    rewind(lol);
    while (!feof(lol))
    {
        binne = fgetc(lol);
        itoa(binne, hehe, 2);
        fprintf(bin, "%s ", hehe);
    }
    */

    //to'rt
    // FILE *oxirgi = fopen("salom.txt", "w");
    // char str[1000];
    // puts("So'z kiriting");
    // scanf("%[^\n]s", str);
    // rewind(oxirgi);
    // oxirgi = fopen("salom.txt", "r");
    // while (!feof(oxirgi))
    // {
    //     fscanf(oxirgi, "%s", str);
    // }
    // fclose(oxirgi);
    // for (int i = 0; i < 2; i++)
    // {
    //     printf("%s\n", str);
    // }




    // FILE *new = fopen("new.txt", "w+");
    // FILE *result = fopen("result.txt", "w");
    // int n;
    // char str[1000];
    // puts("Matnni kiriting");
    // scanf("%[^\n]s", str);
    // fprintf(new, "%s", str);
    // puts("Sonni kiriting");
    // scanf("%d", &n);
    // char harf;
    // rewind(new);
    // while ((harf = fgetc(new))!=EOF)
    // {
    //     for (int i = 0; i < n; i++)
    //     {
    //         fprintf(result, "%c", harf);
    //     }
        
    // }
    

    // FILE *new = fopen("new.txt", "w+");
    // FILE *result = fopen("result.txt", "w");
    // int n;
    // char str[1000];
    // puts("Matnni kiriting");
    // scanf("%[^\n]s", str);
    // fprintf(new, "%s", str);
    // puts("Sonni kiriting");
    // scanf("%d", &n);
    // char harf;
    // rewind(new);
    // int j = 0;
    // char ttp[1000] = {};
    // while ((harf = fgetc(new))!=EOF)
    // {
    //     for (int i = 0; i < n; i++)
    //     {
    //         ttp[j++] = harf;
    //     }
    // }
    // for (int i = 0; i < n; i++)
    // {
    //     fprintf(result, "%s\n", ttp);

    // }
    





























































}